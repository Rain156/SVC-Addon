# 开发约定

## 代码

- 包名前缀统一为 `io.github.rain156.svcaddon`，模块内按领域组织。加载器专用代码不能进入 `api`、`core` 或 `voicechat`。
- 公共配置、网络输入、数值和持久化数据均在边界验证。业务规则使用强类型；避免反射适配、字符串驱动的类型分支和万能工具类。
- 世界操作、注册和命令执行在游戏线程进行。音频回调不能进行文件、网络请求、区块加载或无界计算。
- 编码器、过滤器与播放状态按流持有，显式释放。资源索引与玩家设置按服务器世界隔离。
- Java 使用 4 空格缩进，资源与工作流使用 2 空格，UTF-8、LF；Windows 启动脚本使用 CRLF。

## 变更验证

新增业务规则编写可观察行为测试；为隐私边界、权限、并发、数值边界、持久化失败与协议处理保留回归用例。文案和其他低风险可逆修改无需机械添加测试。

共享源码变更运行两个游戏版本的构建；只改一个加载器也必须验证两个 Minecraft 版本。不要把元数据接受某版本当作已经实现支持，不通过替代版本或空类让 CI 假绿。

提交前更新对应需求状态与测试说明。编译、自动测试、启动和多人实测分别报告。未经真人语音验收的产物仅作为开发构建，不能标记为稳定发布。

## 提交与资源

提交聚焦最终行为，说明修改原因、验证与实际限制。构建目录、日志、运行世界、缓存、音频录制和密钥不得提交。版本升级只修改目标依赖配置和必要适配代码。

新增美术先核对具体资源许可，再更新 `docs/assets.md`。保留第三方署名和许可文本。
